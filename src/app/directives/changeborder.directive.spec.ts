import { ChangeborderDirective } from './changeborder.directive';

describe('ChangeborderDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeborderDirective();
    expect(directive).toBeTruthy();
  });
});
